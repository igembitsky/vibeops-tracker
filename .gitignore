node_modules/
*.log
.DS_Store
.playwright-cli/

# Local tracker data: the real, machine-local issue store. Not versioned with
# the code (it's a database, not source). Only the empty dir is kept.
data/*
!data/.gitkeep

# Local design artifacts (mockups, HTML sketches). Browsable via the design
# browser (agent-os/design-browser), organized like docs/superpowers/specs,
# but not versioned: they are working sketches, not source.
docs/mockups/

# Personal editor settings
.vscode/
